{{ $packages := list 
  "curl" 
  "openssh"
  "zsh"
  "git"
  "build-essential"-}}


{{ $sudo := "sudo " -}}
{{ if eq .UserName "root" -}}
  {{ $sudo = "" -}}
{{ end -}}


#!/bin/bash

set -eufo pipefail

{{ $sudo }}apt-get update
{{ $sudo }}apt-get install -y {{ join $packages " " }}
